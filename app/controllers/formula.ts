// import html from ;

    // var f = ;

    // window.onload = () => {
    //     function transformToCelsius(f: number) {
    //         return (((f - 32) / 9) * 5);
    //     }

    //     function transformToFahrenheit(f: number) {
    //         return ((f / 5) * 9) + 32;
    //     }
    // }
